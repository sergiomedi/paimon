"""Infrastructure layer: adapters implementing the domain's ports.

Every module here is an implementation detail. Nothing outside this package may
import from it, with the single exception of the composition root.
"""
