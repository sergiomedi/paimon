# Measured run — dev

| | |
|---|---|
| when | 2026-09-12 15:25 UTC |
| region | swedencentral |
| api | https://ca-paimon-api-dev.victoriousbeach-d48579f4.swedencentral.azurecontainerapps.io |
| image | crpaimonpym6kgwbeey5w.azurecr.io/paimon-api:8f886d2 |
| database | psql-paimon-pym6kgwbeey5w.postgres.database.azure.com |

**Cold start: 28.467054s** (HTTP 200, ok).

No replica was running: this includes the image pull, the process start and the
first connection pool. It is measurable exactly once per idle period.

**Warm readiness**: 0.300859 0.259884 0.268703 0.229494 0.229814 seconds.

Readiness rather than liveness on purpose: it opens the database, the cache and the
model endpoint, so it is the cheapest request that touches everything.

**Unauthenticated call**: HTTP 401 (401 is the expected answer).

**Authenticated call**: HTTP 200 in 0.301624s — ok.

**Ingestion**: HTTP 200 in 0.255579s.

**unchanged**, 0 chunks. The content hash matched, so nothing was parsed, embedded or written: ingestion is idempotent by document id, and this is what that costs on a repeat — a hash comparison. Compare it with the first ingestion of the same document in an earlier run.

**First answer**: HTTP 200 in 1.749547s.

> *How long do I have to acknowledge a page, and what happens if I do not?*

grounded · 1 citations · 6 retrieved · strategy fused · 656 tokens (611 in, 45 out) · paimon-chat

> You have to acknowledge a page within five minutes. If a page is still unacknowledged after ten minutes, it escalates to the secondary, and after twenty minutes it escalates to the engineering manager [1].

**Same question again**: 1.237687s, against 1.749547s.

**Expect no improvement**, and read a difference either way as noise. Redis holds an
embedding cache, not an answer cache (ADR-0039): the question's embedding is served
from memory the second time, and everything expensive — retrieval and a full
generation — happens again. The first run of this said the gap was a semantic cache,
which does not exist here; the second answer came back *slower* and said so.

What this pair does establish is that the answer is reproducible and that nothing in
the path degrades on a second call.

**Requests in Application Insights in the last 30 minutes**: 20.

## What is still to be filled in by hand

- **Actual spend**, from Cost Management in the portal, per service. Not the
  estimate — the invoice is the only authority on what this cost.
- **The hybrid benchmark** against the real models and search service, per
  `docs/deployment.md`. That is the number worth quoting: it was measured against
  what a deployment would actually use.
- **One span tree**, from Application Insights' transaction view, to show what a
  single request looks like end to end.

