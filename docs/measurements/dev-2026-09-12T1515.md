# Measured run — dev

| | |
|---|---|
| when | 2026-09-12 15:15 UTC |
| region | swedencentral |
| api | https://ca-paimon-api-dev.victoriousbeach-d48579f4.swedencentral.azurecontainerapps.io |
| image | crpaimonpym6kgwbeey5w.azurecr.io/paimon-api:8f886d2 |
| database | psql-paimon-pym6kgwbeey5w.postgres.database.azure.com |

**First request: 0.224396s** (HTTP 200, ok).

1 replica(s) were already running, so this is *not* a cold start. To
measure one, leave the environment idle for the scale-to-zero cooldown — about
five minutes — and run this again.

**Warm readiness**: 0.231589 0.242722 0.223963 0.236843 0.251354 seconds.

Readiness rather than liveness on purpose: it opens the database, the cache and the
model endpoint, so it is the cheapest request that touches everything.

**Unauthenticated call**: HTTP 401 (401 is the expected answer).

**Authenticated call**: HTTP 200 in 0.513089s — ok.

**Ingestion**: HTTP 200 in 0.837900s — {"document_id":"oncall-handbook","chunks_indexed":6,"unchanged":false}

Chunked, embedded through Azure OpenAI and indexed into PostgreSQL over a private
endpoint, as one request.

**First answer**: HTTP 200 in 1.858150s.

> *How long do I have to acknowledge a page, and what happens if I do not?*

grounded · 1 citations · 6 retrieved · strategy fused · 656 tokens (611 in, 45 out) · paimon-chat

> You have to acknowledge a page within five minutes. If a page is still unacknowledged after ten minutes, it escalates to the secondary, and after twenty minutes it escalates to the engineering manager [1].

**Same question again**: 1.960192s, against 1.858150s.

The gap is the semantic cache and whatever the model's own caching contributes; the
cache is per replica (ADR-0039), so at one replica this is its best case rather than
its average.

**Requests in Application Insights in the last 30 minutes**: 7.

## What is still to be filled in by hand

- **Actual spend**, from Cost Management in the portal, per service. Not the
  estimate — the invoice is the only authority on what this cost.
- **The hybrid benchmark** against the real models and search service, per
  `docs/deployment.md`. That is the number worth quoting: it was measured against
  what a deployment would actually use.
- **One span tree**, from Application Insights' transaction view, to show what a
  single request looks like end to end.

